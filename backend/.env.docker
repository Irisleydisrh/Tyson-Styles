# Docker Environment
# Using local PostgreSQL which already has data seeded

# JWT - change in production!
JWT_SECRET=dev-jwt-secret-key-tyson-styles-2024

# Frontend URL
FRONTEND_URL=http://localhost:5173
